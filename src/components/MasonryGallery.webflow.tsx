import MasonryGallery from "./MasonryGallery";
import { props } from "@webflow/data-types";
import { declareComponent } from "@webflow/react";

export default declareComponent(MasonryGallery, {
    name: "Masonry Gallery",
    props: {},
});
